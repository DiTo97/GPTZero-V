[project]
name = "GPTZero-V"
version = "0.1.0"
description = "heuristic GPTZero for the detection of images generated by AI models from metadata"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "c2pa-python>0.5,<1",
  "cryptography<45",
  "exif>1,<2",
  "streamlit>1,<2"
]
